[project]
name = "mvp-trader"
version = "0.1.0"
description = "Multi-pipeline trading skeleton: RAW ingest + MR + Trend + ETF rotation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["trader"]
